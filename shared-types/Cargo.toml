[package]
name = "shared-types"
version = "0.1.0"
edition = "2021"
description = "Wire format shared between engine (server) and web-ui (WASM client)"

[dependencies]
serde = { version = "1", features = ["derive"] }
